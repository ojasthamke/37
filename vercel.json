{
  "buildCommand": "if [ -d \"flutter\" ]; then cd flutter && git fetch --depth 1 && git reset --hard origin/stable && cd ..; else git clone --depth 1 -b stable https://github.com/flutter/flutter.git; fi && ./flutter/bin/flutter config --enable-web && ./flutter/bin/flutter build web --release --no-tree-shake-icons",
  "outputDirectory": "build/web",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
